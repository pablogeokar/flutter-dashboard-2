import 'package:flutter/material.dart';

class SaidasScreen extends StatelessWidget {
  const SaidasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Tela de Saídas'));
  }
}
