import 'package:flutter/material.dart';

class EntradasScreen extends StatelessWidget {
  const EntradasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Tela de Entradas'));
  }
}
